[tool.poetry]
name = "auth-service"
version = "0.1.0"
description = ""
authors = ["Andrew Ermolenko <hoggadar@ya.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
fastapi = {extras = ["standard"], version = "^0.118.0"}
uvicorn = "^0.37.0"
pydantic-settings = "^2.11.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
