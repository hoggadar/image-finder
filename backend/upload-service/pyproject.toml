[tool.poetry]
name = "upload-service"
version = "0.1.0"
description = ""
authors = ["Andrew Ermolenko <hoggadar@ya.ru>"]
packages = [{include = "upload_service", from = "src"}]

[tool.poetry.dependencies]
python = "^3.12"
fastapi = {extras = ["standard"], version = "^0.118.0"}
uvicorn = "^0.37.0"
pydantic-settings = "^2.11.0"
aio-pika = "^9.4.1"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
